# 未办事项

项目待完成事项的简要记录。

## 支付

- [ ] 微信支付未接入

## 对象存储与缩略图

- [x] 接入腾讯云 COS（环境变量已配置：`TENCENT_COS_REGION` / `TENCENT_COS_BUCKET` / `TENCENT_COS_DOMAIN`，鉴权复用 `TENCENT_SECRET_ID` / `TENCENT_SECRET_KEY`）
- [x] 保存设计时生成缩略图并上传 COS，将 URL 写入 `previewUrl`
- [x] 缩略图格式：WebP（quality ~82）或 PNG；宽度约 480px，与高清导出分开存储
- [x] 后端提供上传接口（或预签名直传），前端在 `SaveDesignButton` 保存流程中调用
